jest.dontMock('../Document.js');

describe('Document', function () {
  // var Document = require('../Document.js');

  it('find node position with offset', function () {
    expect(true).toEqual(true);
  });
});
